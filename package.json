{
  "name": "ibackup",
  "version": "1.0.0",
  "description": "A dockerized iPhone backup utility written in node js that uses libimobiledevice and netmuxd to backup your phone over your local network.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "altaybaran@gmail.com",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.9.4",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
